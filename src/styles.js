import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    flex: 1,
  },
  counterView: {
    alignItems: 'center',
    justifyContent: 'center',
    flex: 1,
  },
});
